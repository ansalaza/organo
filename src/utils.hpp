#include <string>
#include <vector>

void homopolymer_compressor(
	const char* ptr, 
	std::string& str
	);

void group_count_task(
	std::vector<int>& elements, 
	std::vector<std::pair<int,int>>& counts
	);